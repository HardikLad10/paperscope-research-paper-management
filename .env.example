# Backend Environment Variables

# Server Configuration
PORT=4000
NODE_ENV=development

# Database Configuration
# For local development: use localhost
# For GCP Cloud SQL: use /cloudsql/PROJECT_ID:REGION:INSTANCE_NAME or public IP
DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=root
DB_PASSWORD=your_password_here
DB_NAME=research_paper_review_db

# Vertex AI Configuration (for AI recommendations)
GCP_PROJECT_ID=your-gcp-project-id
GCP_LOCATION=us-central1
VERTEX_AI_MODEL=gemini-1.5-flash
GOOGLE_APPLICATION_CREDENTIALS=/path/to/service-account-key.json

# Optional: JWT Secret (if implementing authentication)
# JWT_SECRET=your-secret-key-here
